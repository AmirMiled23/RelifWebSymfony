{% extends 'base.html.twig' %}

{% block title %}New ReservationMateriel{% endblock %}

{% block body %}
    <h1>Ajouter une nouvelle Reservation materiel</h1>
    <div style="margin-top: 60px;">
    </div>

    {% for message in app.flashes('error') %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
    {% endfor %}

    {{ include('reservation_materiel/_form.html.twig') }}

    <a href="{{ path('app_reservation_materiel_index') }}">back to list</a>
{% endblock %}
