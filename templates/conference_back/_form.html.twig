{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary btn-lg w-100 mt-3" style="font-weight:600;letter-spacing:1px;">
        {{ button_label|default('Créer la conférence') }}
        <i class="fas fa-plus-circle ms-2"></i>
    </button>
{{ form_end(form) }}
