<form method="post" action="{{ path('app_materiel_delete', {'id_materiel': materiel.idMateriel}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiel.idMateriel) }}">
    <button class="btn btn-danger">Delete</button>
</form>
