php bin/console debug:twig-template templates/conference/back/index.html.twig{% extends 'base.html.twig' %}

{% block title %}Conference Dashboard{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        /* Ensure the statistics button is visible */
        .stats-btn {
            background-color: #ff7b25 !important;
            color: white !important;
            font-weight: bold;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            border: none;
        }
        .stats-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0,0,0,0.15);
        }
        .mega-visible-stats-btn {
            background-color: #ff0000 !important; /* Bright red */
            color: white !important;
            font-weight: bold;
            font-size: 14px;
            animation: pulse 2s infinite;
            border: 3px solid yellow;
            margin-left: 10px;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
    </style>
{% endblock %}

{% block body %}
    <div class="dashboard">
        <h1>Dashboard</h1>
        <p>Relif Dashboard</p>

        <div class="d-flex justify-content-end mb-3">
            <a href="{{ path('app_conference_resources') }}" class="btn btn-primary">Go to Resources</a>
            <button type="button" class="btn mega-visible-stats-btn" data-bs-toggle="modal" data-bs-target="#statisticsModal">
                STATISTICS
            </button>
        </div>
        
        <!-- Add another button below for extra visibility -->
        <div class="d-flex justify-content-end mb-4">
            <button type="button" class="btn btn-lg stats-btn" data-bs-toggle="modal" data-bs-target="#statisticsModal">
                <i class="bi bi-graph-up-arrow me-2"></i> View Conference Analytics
            </button>
        </div>

        <!-- Topic, Speaker, Location search fields -->
        <div class="row mb-3">
            <!-- ...existing code... -->
        </div>

        <!-- View Calendar and Print buttons -->
        <!-- ...existing code... -->

        <!-- Conference table -->
        <!-- ...existing code... -->
    </div>

    <!-- Statistics Modal -->
    <div class="modal fade" id="statisticsModal" tabindex="-1" aria-labelledby="statisticsModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-xl modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title" id="statisticsModalLabel">
                        <i class="bi bi-graph-up me-2"></i> Conference Statistics
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body p-0">
                    <!-- Tabs navigation -->
                    <ul class="nav nav-tabs nav-fill" id="statsTab" role="tablist">
                        <li class="nav-item" role="presentation">
                            <button class="nav-link active" id="overview-tab" data-bs-toggle="tab" data-bs-target="#overview" type="button" role="tab" aria-controls="overview" aria-selected="true">Overview</button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="themes-tab" data-bs-toggle="tab" data-bs-target="#themes" type="button" role="tab" aria-controls="themes" aria-selected="false">Themes</button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="locations-tab" data-bs-toggle="tab" data-bs-target="#locations" type="button" role="tab" aria-controls="locations" aria-selected="false">Locations</button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" id="revenue-tab" data-bs-toggle="tab" data-bs-target="#revenue" type="button" role="tab" aria-controls="revenue" aria-selected="false">Revenue</button>
                        </li>
                    </ul>
                    
                    <!-- Tab content -->
                    <div class="tab-content p-4" id="statsTabContent">
                        <!-- Overview Tab -->
                        <div class="tab-pane fade show active" id="overview" role="tabpanel" aria-labelledby="overview-tab">
                            <div class="row">
                                <div class="col-md-7">
                                    <div class="card shadow-sm h-100">
                                        <div class="card-header bg-white">
                                            <h5 class="mb-0">Monthly Conferences</h5>
                                        </div>
                                        <div class="card-body">
                                            <canvas id="monthlySummaryChart" height="250"></canvas>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-5">
                                    <div class="card shadow-sm h-100">
                                        <div class="card-header bg-white">
                                            <h5 class="mb-0">Conference Status</h5>
                                        </div>
                                        <div class="card-body d-flex justify-content-center align-items-center">
                                            <canvas id="statusPieChart" width="250" height="250"></canvas>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Other tabs... -->
                        <div class="tab-pane fade" id="themes" role="tabpanel" aria-labelledby="themes-tab">
                            <!-- Themes content -->
                            <div class="text-center py-5">Loading theme statistics...</div>
                        </div>
                        
                        <div class="tab-pane fade" id="locations" role="tabpanel" aria-labelledby="locations-tab">
                            <!-- Locations content -->
                            <div class="text-center py-5">Loading location statistics...</div>
                        </div>
                        
                        <div class="tab-pane fade" id="revenue" role="tabpanel" aria-labelledby="revenue-tab">
                            <!-- Revenue content -->
                            <div class="text-center py-5">Loading revenue statistics...</div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary" id="downloadStats">
                        <i class="bi bi-download me-2"></i> Download Report
                    </button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js"></script>
    <script src="{{ asset('js/conference-statistics.js') }}"></script>
    <script>
        // Ensure button is visible and working
        document.addEventListener('DOMContentLoaded', function() {
            console.log('Page loaded, checking statistics button...');
            const statsBtn = document.getElementById('statisticsButton');
            if (statsBtn) {
                console.log('Statistics button found!');
                statsBtn.addEventListener('click', function() {
                    console.log('Statistics button clicked!');
                });
            } else {
                console.error('Statistics button not found in DOM!');
            }
        });

        // Add a console message and a visible alert to debug
        document.addEventListener('DOMContentLoaded', function() {
            console.log('DASHBOARD PAGE LOADED - LOOKING FOR STATISTICS BUTTON');
            
            // Create a temporary notification
            const notification = document.createElement('div');
            notification.style.position = 'fixed';
            notification.style.top = '50%';
            notification.style.left = '50%';
            notification.style.transform = 'translate(-50%, -50%)';
            notification.style.padding = '20px';
            notification.style.background = 'red';
            notification.style.color = 'white';
            notification.style.zIndex = '9999';
            notification.textContent = 'DEBUG: Template loaded. Check console.';
            document.body.appendChild(notification);
            
            setTimeout(() => notification.remove(), 3000);
            
            // Alert the user to check files
            alert('Statistics button should appear next to "Go to Resources". If not visible, check the template file path.');
        });
    </script>
{% endblock %}
